junit:
  jupiter:
    execution:
      parallel:
        enabled: true

spring:
  r2dbc:
    url: r2dbc:tc:postgresql:///discogs?TC_IMAGE_TAG=15


logging:
  level:
    io.r2dbc.postgresql.QUERY: DEBUG # for queries
    io.r2dbc.postgresql.PARAM: DEBUG # for parameters